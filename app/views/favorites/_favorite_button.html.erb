<% if current_user.fav_book?(@book) %>
  <%= link_to "Unfavorite", favorite_path(id: @book.id),  method: :delete,  class: 'btn btn-danger btn-sm' %> 
<% else %>
  <%= link_to "Favorite", {controller: "toppages", action: "iine", book_id: @book.id}, class: 'btn btn-primary btn-sm' %> 
<% end %>